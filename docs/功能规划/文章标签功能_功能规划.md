# 文章标签功能规划

## 1. 功能概述

### 目标
为文章发布功能添加标签支持，让商户/用户在发布文章时可以添加标签，便于文章分类、检索和展示。

### 业务场景
- 商户发布文章时可以选择或创建标签
- 用户可以通过标签筛选文章
- 文章详情页展示相关标签
- 提升文章的可发现性和分类管理

### 与现有系统关系
- 与文章模块（la_article）关联
- 与商户模块（la_merchant）关联
- 复用现有的API架构和前端组件风格

---

## 2. 功能结构

### 模块划分
```
文章标签功能
├── 后端模块
│   ├── 标签管理（增删改查）
│   ├── 文章-标签关联
│   └── 标签统计（文章数量）
├── 前端模块
│   ├── 商户端：发布文章时选择/创建标签
│   ├── 用户端：文章列表标签展示
│   └── 管理后台：标签管理页面
```

### 页面/接口划分
| 端 | 功能 | 说明 |
|---|---|---|
| 商户端 | 发布文章页 | 添加标签选择/输入组件 |
| 用户端 | 文章详情页 | 显示文章标签 |
| 用户端 | 文章列表页 | 支持按标签筛选 |
| 管理后台 | 标签管理页 | 标签增删改查 |

---

## 3. 数据库设计

### 3.1 标签表 `la_article_tag`

```sql
CREATE TABLE `la_article_tag` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT '标签ID',
  `name` varchar(32) NOT NULL DEFAULT '' COMMENT '标签名称',
  `click_count` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '点击次数',
  `article_count` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '文章数量',
  `sort` int(11) NOT NULL DEFAULT '0' COMMENT '排序（越大越靠前）',
  `is_hot` tinyint(1) unsigned NOT NULL DEFAULT '0' COMMENT '是否热门：0-否 1-是',
  `is_show` tinyint(1) unsigned NOT NULL DEFAULT '1' COMMENT '是否显示：0-隐藏 1-显示',
  `create_time` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '创建时间',
  `update_time` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '更新时间',
  `delete_time` int(11) unsigned DEFAULT NULL COMMENT '删除时间',
  PRIMARY KEY (`id`),
  KEY `idx_name` (`name`),
  KEY `idx_article_count` (`article_count`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章标签表';
```

### 3.2 文章标签关联表 `la_article_tag_relation`

```sql
CREATE TABLE `la_article_tag_relation` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `article_id` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '文章ID',
  `tag_id` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '标签ID',
  `create_time` int(11) unsigned NOT NULL DEFAULT '0' COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_article_tag` (`article_id`, `tag_id`),
  KEY `idx_tag_id` (`tag_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章标签关联表';
```

### 3.3 文章表修改

```sql
-- 为文章表添加标签字段（可选，用于缓存标签名）
ALTER TABLE `la_article` ADD COLUMN `tags` varchar(255) DEFAULT '' COMMENT '标签（逗号分隔）' AFTER `issue_status`;
```

---

## 4. 后端文件规划

### 4.1 模型层
| 文件路径 | 说明 |
|---|---|
| `app/common/model/article/ArticleTag.php` | 标签模型 |
| `app/common/model/article/ArticleTagRelation.php` | 标签关联模型 |

### 4.2 控制器层
| 文件路径 | 说明 |
|---|---|
| `app/adminapi/controller/article/ArticleTagController.php` | 后台标签管理控制器 |
| `app/api/controller/ArticleTagController.php` | 前端标签API控制器 |

### 4.3 逻辑层
| 文件路径 | 说明 |
|---|---|
| `app/adminapi/logic/article/ArticleTagLogic.php` | 后台标签逻辑 |

### 4.4 验证器
| 文件路径 | 说明 |
|---|---|
| `app/adminapi/validate/article/ArticleTagValidate.php` | 标签验证规则 |

### 4.5 列表类
| 文件路径 | 说明 |
|---|---|
| `app/adminapi/lists/article/ArticleTagLists.php` | 后台标签列表 |

### 4.6 需修改的现有文件
| 文件路径 | 修改内容 |
|---|---|
| `app/api/controller/merchant/ArticleController.php` | save方法添加标签处理 |
| `app/adminapi/logic/article/ArticleLogic.php` | 添加标签关联处理 |

---

## 5. 前端文件规划

### 5.1 管理后台
| 文件路径 | 说明 |
|---|---|
| `admin/src/views/article/tag/index.vue` | 标签列表页 |
| `admin/src/views/article/tag/edit.vue` | 标签编辑弹窗 |
| `admin/src/api/article.ts` | 添加标签相关API |

### 5.2 移动端（商户端）
| 文件路径 | 说明 |
|---|---|
| `uniapp/src/packages/pages/merchant/article_add.vue` | 添加标签选择组件 |
| `uniapp/src/components/business/TagSelector.vue` | 标签选择器组件（新建） |

### 5.3 移动端（用户端）
| 文件路径 | 说明 |
|---|---|
| `uniapp/src/pages/news_detail/news_detail.vue` | 显示文章标签 |
| `uniapp/src/components/business/ArticleList.vue` | 列表项显示标签 |

---

## 6. 接口规划

### 6.1 后台管理接口

| 接口路径 | 方法 | 参数 | 返回值 | 权限 | 说明 |
|---|---|---|---|---|---|
| `/article.tag/lists` | GET | page_no, page_size, name | {lists, count} | 需登录 | 标签列表 |
| `/article.tag/all` | GET | - | [{id, name}] | 需登录 | 所有标签（下拉用） |
| `/article.tag/add` | POST | name, sort, is_hot, is_show | - | 需登录 | 添加标签 |
| `/article.tag/edit` | POST | id, name, sort, is_hot, is_show | - | 需登录 | 编辑标签 |
| `/article.tag/delete` | POST | id | - | 需登录 | 删除标签 |
| `/article.tag/detail` | GET | id | {} | 需登录 | 标签详情 |

### 6.2 前端API接口

| 接口路径 | 方法 | 参数 | 返回值 | 权限 | 说明 |
|---|---|---|---|---|---|
| `/articleTag/lists` | GET | page_no, page_size | {lists, count} | 公开 | 标签列表（用户端） |
| `/articleTag/hot` | GET | limit | [{id, name}] | 公开 | 热门标签 |
| `/articleTag/create` | POST | name | {id, name} | 需登录 | 创建标签（商户） |

### 6.3 商户端接口修改

| 接口路径 | 方法 | 新增参数 | 说明 |
|---|---|---|---|
| `/merchant.article/save` | POST | tag_ids: string (逗号分隔的标签ID) | 发布文章时关联标签 |
| `/merchant.article/detail` | GET | - | 返回值增加 tag_list 字段 |

---

## 7. 测试步骤

### 7.1 创建标签数据
1. 登录管理后台
2. 进入「文章管理」->「标签管理」
3. 点击「添加标签」，输入标签名称如"技术教程"
4. 重复添加多个标签："热门推荐"、"新手入门"、"高级技巧"

### 7.2 商户发布文章选择标签
1. 登录商户端APP
2. 进入「发布文章」页面
3. 填写文章标题、内容
4. 在标签区域点击选择标签
5. 可选择已有标签或输入新标签
6. 点击发布

### 7.3 验证文章标签显示
1. 进入文章详情页
2. 确认标签显示正确
3. 点击标签可跳转到相关文章列表

### 7.4 验证标签统计
1. 发布多篇文章选择相同标签
2. 在后台标签管理查看文章数量统计
3. 删除文章后确认统计数量更新

---

## 8. 开发阶段划分

### 第一阶段：基础功能（优先级：高）
- [ ] 数据库表创建
- [ ] 后端标签CRUD接口
- [ ] 商户端发布文章添加标签选择
- [ ] 文章详情显示标签

### 第二阶段：完善功能（优先级：中）
- [ ] 后台标签管理页面
- [ ] 标签文章数量统计
- [ ] 热门标签功能
- [ ] 用户端按标签筛选文章

### 第三阶段：优化体验（优先级：低）
- [ ] 标签云展示
- [ ] 标签自动推荐
- [ ] 标签SEO优化

---

## 9. 进度记录

| 阶段 | 任务 | 状态 | 完成时间 |
|---|---|---|---|
| 第一阶段 | 数据库表创建 | 待开始 | - |
| 第一阶段 | 后端接口开发 | 待开始 | - |
| 第一阶段 | 前端页面开发 | 待开始 | - |
| 第二阶段 | 后台管理页面 | 待开始 | - |
| 第二阶段 | 统计功能 | 待开始 | - |

---

## 10. 注意事项

1. **标签数量限制**：每篇文章最多选择5个标签
2. **标签名称规范**：2-10个字符，不能包含特殊符号
3. **标签去重**：相同名称的标签自动合并（不区分大小写）
4. **删除保护**：有文章关联的标签不能直接删除，需先解除关联
5. **性能优化**：文章表tags字段用于缓存，减少关联查询
